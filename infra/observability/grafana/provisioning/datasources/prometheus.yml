apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus # stable uid so the hand-authored dashboard JSON can reference it directly
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
